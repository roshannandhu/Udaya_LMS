node_modules/
.venv
venv/
env/
__pycache__/
*.pyc
.DS_Store
dist/
build/
*.log
broadcasts.json
tmp/
backend/generated/

# Cloudflare Origin cert + key on the EC2 box — NEVER commit (private key).
cf-origin/

# backend/.env holds the Supabase service_role key + Zoom/Resend/Gemini secrets.
# NOT committed — production secrets live only on the EC2 box
# (/opt/udaya/backend/.env) and on local dev machines. Template: backend/.env.example
backend/.env

# frontend/.env.local (Supabase anon key + dev API URL) stays committed —
# the anon key is public by design (it ships in the frontend bundle anyway).

# teacher_settings.json holds a private AI (Gemini) API key — NOT committed
# (GitHub push protection blocks it). Each user adds their own AI key in the
# teacher Settings page if they want the AI mentorship feature.
teacher_settings.json

# ai_insights_cache.json is the server-side AI mentor response cache —
# regenerated data, never committed.
ai_insights_cache.json

# whatsapp_config.json holds the WhatsApp provider (WANotifier) API key + sender
# number — server-only secret, NOT committed. Each teacher adds their own key in
# the WhatsApp → Settings tab.
whatsapp_config.json
backend/whatsapp_config.json

# Screenshots & Images
screenshots/
*.png
*.jpg
*.jpeg

# ...but the app's own brand/icon assets in public/ MUST ship to Vercel
!frontend/public/*.png
!playstore/*.png
!frontend/public/*.jpg
!frontend/public/*.jpeg

# Test tokens & scripts
.test_token
verify_run.mjs
verify_test.js
verify_test.mjs
subjects_page_redesign.html
*.timestamp-*.mjs

.vercel

# Android signing & Firebase — NEVER commit.
# Losing the keystore (or committing it) means you can never update the app again.
*.jks
*.keystore
google-services.json
android/app/build/
android/.gradle/

# stray build artifact from manual Pages upload
frontend/udaya-dist.zip

# Android build artifacts / IDE
*.apk
*.aab
frontend/android/.idea/

# Keystore files
udaya-release.keystore
keystore_base64.txt

# Secret-bearing helper scripts (contain keystore passwords + R2 keys in plaintext).
# NEVER commit — set the values as GitHub Secrets, then delete the script.
upload_secrets.bat
push_secrets.py
*secrets*.bat
*secrets*.sh
*secrets*.py
r2-upload/
_design_handoff/
upload_to_r2.py
r2-upload/
_r2cors.py
